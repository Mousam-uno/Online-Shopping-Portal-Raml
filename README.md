# Online-Shopping-Portal-Raml

API endpoints

-/order

   -(post) -> to create an order
  
-/order/{orderID} 

  - (get) -> to get order details
  - (put) -> to update an order
  - (delete) -> to delete an order
